{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Syronius FRAME latest.json (V1)",
  "type": "object",
  "additionalProperties": false,
  "required": ["updated_at", "date_folder"],
  "properties": {
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of the most recent successful publish. e.g. 2025-12-29T03:14:07Z"
    },
    "date_folder": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Active day folder name. e.g. 2025-12-29"
    },
    "latest_base": {
      "type": ["string", "null"],
      "description": "Base name (no extension, no path) of the most recently published image. Null if no image published today. Connects all sidecars: <base>.jpg, <base>.txt, <base>.orientation, <base>.ready"
    },
    "count_today": {
      "type": "integer",
      "minimum": 0,
      "description": "Total images successfully published today."
    }
  }
}
